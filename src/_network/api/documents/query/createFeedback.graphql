mutation createFeedback(
  $comment: String!
  $rating: Int!
  $uid: String!
  $email: String!
) {
  createFeedback(
    data: {
      comment: $comment
      rating: $rating
      firebaseUserId: $uid
      email: $email
    }
  ) {
    id
  }
}

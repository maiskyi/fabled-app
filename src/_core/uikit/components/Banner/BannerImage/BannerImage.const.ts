import { TWO_ROBOTS } from '../../../constants';

import { BannerImageAsset } from './BannerImage.types';

export const ASSETS_MAPPING: Record<BannerImageAsset, string> = {
  'two-robots': TWO_ROBOTS,
};

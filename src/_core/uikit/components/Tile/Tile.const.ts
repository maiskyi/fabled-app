import { Color } from '@ionic/core';
import { Partial } from '@react-spring/web';

export const OUTLINE_COLOR_MAPPING: Partial<Record<Color, string>> = {
  secondary: 'var(--ion-color-secondary)',
};
